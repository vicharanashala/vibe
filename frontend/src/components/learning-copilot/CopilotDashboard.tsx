import LearningCopilot from "@/app/pages/student/learning-copilot";

/** Compatibility entry point for the original post-quiz scaffold. */
export function CopilotDashboard(_props: { data?: unknown }) {
  return <LearningCopilot />;
}
