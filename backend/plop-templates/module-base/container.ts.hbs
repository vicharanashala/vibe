import { ContainerModule } from 'inversify';
import { {{constantCase module}}_TYPES } from './types.js';

export const {{module}}ContainerModule = new ContainerModule(options => {
  // Repositories

  // Services

  // Controllers
});
