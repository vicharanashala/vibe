const {MongoClient,ObjectId}=require('mongodb');const c=new MongoClient('mongodb://127.0.0.1:27017');c.connect().then(async()=>{const db=c.db('vibe');const doc=await db.collection('companions').findOne({userId:'6a4b9f85cc68bde40897fc16'});console.log('realProgress (from enrollments): 33');console.log('lastKnownProgress in DB:',doc?.lastKnownProgress);console.log('newJourney in DB:',doc?.newJourney);await c.close()})
