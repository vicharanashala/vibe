/**
 * Deliberately small (PLANNING.md §6): the client is on record as unsure this
 * feature is worth building big, so v1 is a flat post + one-level reply, no
 * voting, no moderation queue. Extend only if it proves itself.
 */
export const MAX_COMMENT_LENGTH = 2000;
export const DEFAULT_LIST_LIMIT = 100;
