# 2026-07-12 — Session Log

## Companion Growth Logic — Session 2026-07-12

### What was done

1. **Enrollment dashboard bug investigation** — `currentUserChecker` in `backend/src/modules/auth/index.ts` was calling `verifyToken()` (boolean) instead of `getCurrentUserFromToken()` (IUser). Fix applied but not yet verified end-to-end.

2. **Companion sad mood root cause** — `realProgress: 10` was being read from the `enrollment` (singular) collection, not `enrollments`. The stale doc belonged to `sahasra2069@gmail.com` (userId: `6a4b9f85cc68bde40897fc16`). Fix: changed `CompanionService._getRealProgress` to query `enrollments` (plural).

3. **`_deriveMood` fix** — new students with `idleDays=0 && progress=0` now get `mood: neutral` instead of falling through to `happy`.

4. **`_daysSinceLastActivity` empty-array guard** — added `Math.max(0, ...)` and the `if (dates.length === 0) return 0` guard to prevent `Infinity` from spreading into mood logic.

5. **Companion growth logic verified** — stage thresholds `[0,17,33,50,67,83,100]` and AMOOD mood derivation match the prototype exactly.

6. **`neutral` mood frontend gap** — backend had `neutral`, frontend didn't. Added to:
   - `frontend/src/types/companion.ts` — CompanionMood union
   - `frontend/src/components/Companion/CompanionWidget.tsx` — toPrototypeMood switch
   - `frontend/src/components/Companion/companionRenderer.js` — MSGS.neutral + MPILLS.neutral
   - `companionRenderer.js` drawMouth + drawBeak — neutral gets subtle small smile

7. **Sleeping eyes fix** — was spiky/angry-looking. Changed to soft curved downward lashes + blush circles on cheeks.

### Key findings

- **Two browser accounts active**: pandu = `6a4b8c7e1e6b7a91c33fb27c` / sahasra (actual session) = `6a4b9f85cc68bde40897fc16`. The companion being tested was always for sahasra, not pandu.
- **Two enrollments collections**: `enrollment` (singular, legacy/stale) and `enrollments` (plural, correct). `CompanionService._getRealProgress` now correctly queries `enrollments` (plural).
- **Test course deleted**: Course `6a53311b5b3a0f2ab44dc807` (Test Drive: Companion Demo) and all related records fully deleted for pandu. pandu now has 0 enrollments.
- **`CompanionService` queries `enrollments` collection** (plural, correct) after fix. Backend clean, 0 TypeScript errors.

### Companion growth logic (verified correct)

**Stage thresholds:**
```
p >= 83 → Stage 5 (Adult)
p >= 67 → Stage 4
p >= 50 → Stage 3
p >= 33 → Stage 2
p >= 17 → Stage 1
p <  17 → Stage 0 (Baby)
```

**Mood derivation (AMOOD):**
```
p >= 100        → celebrating
idleDays >= 5   → sleeping
idleDays >= 3   → angry
idleDays >= 1   → sad
idleDays === 0 && p === 0 → neutral (new student, no activity)
p >= 40         → excited
else            → happy
```

### Companion is fully automatic

Every field computed live on each `/companion/me` call — no manual support toggle. Only manual action is changing the animal.

### Cross-check bugs found and fixed (2026-07-12 afternoon)
- `frontend/src/types/companion.ts` — `CompanionMood` type was missing entirely (TS error). Added full union including `newJourney`
- `frontend/src/store/companion-store.ts` — structural error: `selectAnimal`/`setStudying`/`clearNewJourney` were floating outside the returned object (missing `,` after `fetchCompanion`). Rewrote store cleanly
- `frontend/src/components/Companion/CompanionWidget.tsx` — `toPrototypeMood` missing `newJourney` case (returned `undefined` → canvas had no mood)
- `frontend/src/components/Companion/CompanionWidget.tsx` — footer bar showed `happy` during newJourney. Now shows `new journey`
- Backend `interfaces.ts` — comment said "≥20 points" but threshold is 15. Fixed comment
- `fetchCompanion` incorrect auto-acknowledge removed — fired on every poll, not just `true→false` transition. Widget handles this correctly.

### Enrollment dashboard bug — still open

`currentUserChecker` fix applied. Needs console.log tracing next session:
1. Log `user._id` type in `EnrollmentController.getUserEnrollments`
2. Log userId in `EnrollmentService.getEnrollments`
3. Or test HTTP directly with a copied Bearer token

### Test accounts
- pandu: `6a4b8c7e1e6b7a91c33fb27c` — 0 enrollments (clean)
- sahasra: `6a4b9f85cc68bde40897fc16` — 0 enrollments (clean), companion Stage 0 / neutral

### Scripts created / updated
- `backend/scripts/_test-stage.cjs` — set percentCompleted, usage: `node _test-stage.cjs 50`
- `backend/scripts/_test-mood.cjs` — set idleDays + progress, usage: `node _test-mood.cjs 3 20`
- `backend/scripts/_reset-companion-test.cjs` — cleanup test data, restore to Stage 0 / neutral
- `backend/scripts/cycle-companion-stages.ps1` — corrected thresholds to [0,17,33,50,67,83,100]
- `backend/scripts/bump-companion-progress.cjs` — corrected to write to `enrollments` (not old schema)
- `backend/scripts/_agg-realprogress-test.cjs`, `_check-sahasra-enrollments.cjs`, `_find-real-enrollment-doc.cjs`, `_delete-enrollment-doc-by-id.cjs`

### End-to-end tests done (user confirmed all pass)
- All 6 stages verified at: 5%, 20%, 40%, 55%, 70%, 85%/100%
- All moods verified at: happy, sad, angry, sleeping, celebrating
- Neutral mood smile: confirmed working
- Sleeping eyes: confirmed cute

---

## Earlier sessions

- See `MEMORY.md` for long-term curated memories
- See daily logs in `memory/` for raw session logs
# 2026-07-12 — Session Log

## Companion Growth Logic — Session 2026-07-12

### What was done

1. **Enrollment dashboard bug investigation** — `currentUserChecker` in `backend/src/modules/auth/index.ts` was calling `verifyToken()` (boolean) instead of `getCurrentUserFromToken()` (IUser). Fix applied but not yet verified end-to-end.

2. **Companion sad mood root cause** — `realProgress: 10` was being read from the `enrollment` (singular) collection, not `enrollments`. The stale doc belonged to `sahasra2069@gmail.com` (userId: `6a4b9f85cc68bde40897fc16`). Fix: changed `CompanionService._getRealProgress` to query `enrollments` (plural).

3. **`_deriveMood` fix** — new students with `idleDays=0 && progress=0` now get `mood: neutral` instead of falling through to `happy`.

4. **`_daysSinceLastActivity` empty-array guard** — added `Math.max(0, ...)` and the `if (dates.length === 0) return 0` guard to prevent `Infinity` from spreading into mood logic.

5. **Companion growth logic verified** — stage thresholds `[0,17,33,50,67,83,100]` and AMOOD mood derivation match the prototype exactly.

6. **`neutral` mood frontend gap** — backend had `neutral`, frontend didn't. Added to:
   - `frontend/src/types/companion.ts` — CompanionMood union
   - `frontend/src/components/Companion/CompanionWidget.tsx` — toPrototypeMood switch
   - `frontend/src/components/Companion/companionRenderer.js` — MSGS.neutral + MPILLS.neutral
   - `companionRenderer.js` drawMouth + drawBeak — neutral gets subtle small smile

7. **Sleeping eyes fix** — was spiky/angry-looking. Changed to soft curved downward lashes + blush circles on cheeks.

8. **`newJourney` system implemented** (server-authoritative, replaces session-ref approach). **Cross-check caught 3 bugs:** `CompanionMood` type missing (TS error), store structural error (methods outside return object), `toPrototypeMood` missing `newJourney` case.
   - `CompanionDoc` interface: added `lastKnownProgress?: number` and `newJourney?: boolean`
   - `CompanionRepository.ts`: added `updateProgressMeta()` (detects ≥15pt drops), `clearNewJourney()`
   - `CompanionService.ts`: `getCompanionState()` calls `updateProgressMeta()` after computing `realProgress`
   - `Companion.toJSON()`: accepts and returns `newJourney: boolean`
   - `ICompanion` interface: added `newJourney: boolean`
   - `CompanionController.ts`: added `PATCH /me/new-journey-seen` endpoint
   - `companionRenderer.js`: added `MSGS.newJourney` (4 messages) and `MPILLS.newJourney` (teal `#e0f7fa/#006064/🚀`)
   - `CompanionWidget.tsx`: replaced old session-ref logic with server-authoritative `companion.newJourney` check
   - `companion-store.ts`: added `newJourney: boolean` to `CompanionState`, added `clearNewJourney()` action
   - Threshold: `realProgress <= prev - 15` (prev must be ≥20 for message to fire)
   - Widget behavior: shows newJourney message one-shot, stays until progress increases (then auto-clears)
   - Backend auto-clears `newJourney` flag when user's `realProgress` increases

### Key findings

- **Two browser accounts active**: pandu = `6a4b8c7e1e6b7a91c33fb27c` / sahasra (actual session) = `6a4b9f85cc68bde40897fc16`
- **Two enrollments collections**: `enrollment` (singular, legacy/stale) and `enrollments` (plural, correct). `CompanionService._getRealProgress` now correctly queries `enrollments` (plural)
- **newJourney message confirmed visible**: "A new journey starts today! 🌟" appeared correctly when progress dropped from 50→33 (Stage 3→2)
- **Footer bar** shows `happy` during newJourney; message text comes from MSGS.newJourney
- **Pill background** rendered on canvas (teal border); DOM `CompanionMessage` renders pill from `MPILLS` map

### Companion growth logic (verified correct)

**Stage thresholds:**
```
p >= 83 → Stage 5 (Adult)
p >= 67 → Stage 4
p >= 50 → Stage 3
p >= 33 → Stage 2
p >= 17 → Stage 1
p <  17 → Stage 0 (Baby)
```

**Mood derivation (AMOOD):**
```
p >= 100        → celebrating
idleDays >= 5   → sleeping
idleDays >= 3   → angry
idleDays >= 1   → sad
idleDays === 0 && p === 0 → neutral (new student, no activity)
p >= 40         → excited
else            → happy
```

### newJourney behavior
- Fires when: user had ≥20% progress, then drops by ≥15 points (e.g., enrolled a new 0% course)
- Shows message from MSGS.newJourney list
- Canvas border goes teal (MPILLS.newJourney)
- Footer: mood name = `'newJourney'` → mapped to `MSGS.newJourney` messages
- Auto-clears: when `realProgress` increases (user starts learning again) → backend clears flag, widget returns to normal mood display

### Test accounts
- pandu: `6a4b8c7e1e6b7a91c33fb27c` — 0 enrollments (clean)
- sahasra: `6a4b9f85cc68bde40897fc16` — 0 enrollments (clean), companion Stage 0 / neutral

### Scripts created / updated
- `backend/scripts/_test-stage.cjs` — set percentCompleted, usage: `node _test-stage.cjs 50`
- `backend/scripts/_test-mood.cjs` — set idleDays + progress, usage: `node _test-mood.cjs 3 20`
- `backend/scripts/_reset-companion-test.cjs` — cleanup test data, restore to Stage 0 / neutral
- `backend/scripts/_test-mixed-progress.cjs` — Phase 1 (avg 50% → Stage 3), Phase 2 (avg 33% → Stage 2, triggers newJourney)
- `backend/scripts/cycle-companion-stages.ps1` — corrected thresholds to [0,17,33,50,67,83,100]
- `backend/scripts/bump-companion-progress.cjs` — corrected to write to `enrollments` (not old schema)
- `backend/scripts/_companion-debug.cjs` — shows lastKnownProgress and newJourney flag
- `backend/scripts/_check-enrollments.cjs` — shows all enrollment percentCompleteds for a user

### End-to-end tests done (user confirmed all pass)
- All 6 stages verified at: 5%, 20%, 40%, 55%, 70%, 85%/100%
- All moods verified at: happy, sad, angry, sleeping, celebrating
- Neutral mood smile: confirmed working
- Sleeping eyes: confirmed cute
- newJourney message: confirmed visible ("A new journey starts today! 🌟") when progress drops 50→33

### Enrollment dashboard bug — still open

`currentUserChecker` fix applied. Needs console.log tracing next session:
1. Log `user._id` type in `EnrollmentController.getUserEnrollments`
2. Log userId in `EnrollmentService.getEnrollments`
3. Or test HTTP directly with a copied Bearer token

---

## Earlier sessions

- See `MEMORY.md` for long-term curated memories
- See daily logs in `memory/` for raw session logs